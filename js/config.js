/* =========================================================
   config.js — paste your Supabase project keys here.
   Get them at: Supabase dashboard → Project Settings → API
     • Project URL      → SUPABASE_URL
     • Project API keys → "anon public"  → SUPABASE_ANON_KEY
   The anon key is SAFE to expose in frontend code (it only allows
   what your Row Level Security policies permit).
   ========================================================= */
window.MINDMAP_CONFIG = {
  SUPABASE_URL: '',      // e.g. https://abcdefghijklmno.supabase.co
  SUPABASE_ANON_KEY: ''  // e.g. eyJhbGciOiJIUzI1NiIsInR5cCI6...
};
